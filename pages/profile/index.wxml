<view class="page">
  <view class="profile">
    <!-- <button class="avatar-btn" open-type="getUserInfo" bindtap="onGetProfile">
      <image class="avatar" src="{{userInfo.avatarUrl || '/assets/me.png'}}" mode="aspectFill" />
    </button>
    <view class="info">
      <text class="name">{{userInfo.nickName || '未登录'}}</text>
      <text class="desc">点击头像授权获取信息</text>
    </view> -->
  </view>

  <view class="panel">
    <!-- <view class="item">
      <text>占用存储</text>
      <text class="muted">{{storageSize}} MB</text>
    </view> -->
    <!-- <view class="item" bindtap="onFeedback">
      <text>意见反馈</text>
      <text class="muted">去填写</text>
    </view> -->
    <view class="item" bindtap="onClearCache">
      <text>清理本地缓存</text>
      <text class="muted warn">清理</text>
    </view>
  </view>

  <!-- <button class="primary" bindtap="onContact">联系客服</button> -->
</view>
